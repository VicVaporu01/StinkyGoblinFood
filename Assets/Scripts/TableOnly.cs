using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TableOnly : MonoBehaviour
{
    public bool available;
    //public GameObject circle;
    //public GameObject triangle;
    //public GameObject cansule;
    //// Start is called before the first frame update
    void Start()
    {
        available = true;
        // circle = GameObject.Find("Circle");
    }

    // Update is called once per frame
    void Update()
    {
        //if(transform.position == circle.transform.position)
        //{
        //    available = false;
        //} else if(transform.position == triangle.transform.position)
        //{
        //    available = false;
        //}
        //else if (transform.position == cansule.transform.position)
        //{
        //    available = false;
        //}

        //if(!circle)
        //{
        //    available = true;
        //}

    }
}
